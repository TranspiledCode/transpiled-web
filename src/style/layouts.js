// layouts.js
const layouts = {
  maxWidth: '2000px',
  sectionPadding: 'clamp(2rem, 3vw, 4rem) clamp(2rem, 6vw, 8rem)',
  sectionPaddingNoTop: '0rem clamp(2rem, 6vw, 8rem)',
};

export default layouts;
